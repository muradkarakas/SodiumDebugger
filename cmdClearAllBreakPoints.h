#pragma once



/*	Returns TRUE if command is executed successfully, otherwise returns FALSE */
BOOL cmdClearAllBreakPoints(TokenCommand* Command);

int
ClearAllBreakPoints(
	TokenCommand* Command
);
